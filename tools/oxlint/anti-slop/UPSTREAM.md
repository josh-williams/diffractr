# Anti-slop provenance

Source: https://github.com/dmmulroy/anti-slop

Commit: `c44ef22ca116d0ba62a3ff663a0bd13a3f3fa40b`

Copied from `skills/install-anti-slop/assets/anti-slop/` at that commit, with every copied file verified byte-for-byte. Installed at `tools/oxlint/anti-slop/`. The root MIT license is included; nested Stylistic license and provenance are preserved.

No rule modifications. All generic rules are enabled; Effect rules are not enabled because this project does not depend on Effect. Vendored source is excluded from project linting and formatting.

The project-owned `../anti-slop.mjs` adapter loads the unmodified TypeScript entry point through `tsx` for Node 22.12 compatibility.
